// Domen qatlami — butun sozlama `sc.kmp-library` convention plugin'idan keladi
// (KMP target'lar, android bloki, namespace, coroutines, `:dev:core:common`, test).
plugins {
    id("sc.kmp-library")
}
